[package]
name = "cita-web3"
version = "0.1.0"
authors = ["Cryptape Technologies <arch@cryptape.com>"]

[dependencies]
jsonrpc-types = { path = "../jsonrpc-types" }
web3 = "0.4.0"

[features]
default = []
